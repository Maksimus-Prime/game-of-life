doctype html
html
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="initial-scale=1.0, width=device-width")
        meta(name="description" content="game")
        meta(name="keywords" content="game")
        meta(name="author" content="Lamonov Maks")
        title Game of Life
    body
        main
            #board
            .inputs
                input(type="text" id="widthInput")
                input(type="text" id="heightInput")
            .buttons
                button(id="startButton") Start
                button(id="pauseButton") Pause
                button(id="restartButton") Restart